# Benchmarks

These measure signature and key encapsulation throughput and hashing rates. The numbers feed two things. They calibrate the gas for the native post quantum opcodes in the virtual machine. They also feed the validator resource budget, meaning the maximum verify time on a reference phone from the year 2020, which the benchmarks repository enforces as a consensus parameter. The benchmark report is committed alongside the first tagged release.
